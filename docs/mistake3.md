the rm -rf command is so destructive i wasn't paying attention and removed a source tarball from which i had to install the next package from (bison), instead of the package folder i just installed (gettext).

i need to pay more attention, thankfully its a very minor mistake, redownloaded the source tarball to the $LFS/sources directory via another login shell through wget.
