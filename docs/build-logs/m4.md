# Package: M4 (Temporary Native Tool)

**Version:** 1.4.21
**Chapter:** 6 (Cross-Compiling Temporary Tools)
**SBU Estimate:** 0.1 SBU

## Overview
M4 is a prerequisite macro processor required for autoconf and other build system generators. This marks the beginning of Chapter 6, where utilities are cross-compiled to run *natively* on the target LFS system rather than the host.

## Security / Custom Modifications
- **Native Target Configuration:** Configured with `--host=$LFS_TGT` to utilize the cross-toolchain built in Chapter 5.
- **Sysroot Installation:** Installed via `make DESTDIR=$LFS install` to correctly map the binary into the target's `/usr/bin` directory, avoiding host contamination.

## Build Notes
- Extracted, configured, and compiled cleanly.
- Source tree successfully destroyed post-installation to maintain a sterile build environment for future passes.
